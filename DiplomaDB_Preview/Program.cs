using DiplomaDB_Preview.Classes;
using DiplomaDB_Preview.Data;
using DiplomaDB_Preview.Models;

class Program
{
    static async Task Main()
    {
        //var context = new DiplomaDbPreviewContext();
        //AsyncRepository<User> UserRepostitory = new(context);
        //UnitOfWork unitOfWork = new(context);
        //UserRegister UR = new(unitOfWork, UserRepostitory);

        //User Usr = new(); -- blank user
        //UR.AddUser(Usr); -- will print error in console because user is not valid
        //UR.DeleteUserById(5); -- will print error in console because user not found

        //User Usr1 = UR.GetUser(4); 
        //foreach (var item in context.Users)
        //{
        //    Console.WriteLine(item.FirstName + item.LastName +$" #{item.Id}");
        //}
    }
}